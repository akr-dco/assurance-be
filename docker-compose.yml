services:
  app:
    build: .
    container_name: assurance-backend-prod
    ports:
      - "8090:8090"
    volumes:
      - ./.env.development:/app/.env.development
      - ./.env.production:/app/.env.production
    environment:
      - APP_ENV=production
